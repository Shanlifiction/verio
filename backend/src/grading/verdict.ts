import { Session } from "../sessions.js";

export interface Verdict {}

export async function getVerdict(session: Session) {
    // const response = await grade<BehaviorGrade>({
    //     session,
    // });

    return /* response ?? */ undefined;
}
